FROM node:22-alpine

WORKDIR /workspace

RUN apk add --no-cache \
    ca-certificates \
    curl \
    git \
    make \
    python3

RUN corepack enable && corepack prepare pnpm@latest --activate

EXPOSE 8000
